# RIQ21 Portal Gap-Fill — No-Trace Capture 2026-05-26

**Source:** passive capture (Ford admin session, zero programmatic fetches — indistinguishable from normal admin use)
**Raw bodies:** `~/web-recon/data/reports/portal_PASSIVE_2026-05-26T18-51-20-706Z/` (143 JSON, all HTTP 200)
**Stale versions backed up to:** `data/_backup-2026-05-26/`

## Staged into `data/` (raw endpoint dumps, `{name,responseCode,message,data}` envelope)
| File | .data rows | Endpoint |
|---|---|---|
| events-customers.json | 6558 | /v1/events/customers/all |
| events-employees.json | 200 | /v1/events/employees/all |
| events-sales.json | 591 | /v1/events/sales/all |
| events-production.json | **MISSING** | 200 but no body saved — needs re-grab |
| fixes.json | 140 | /v1/admin/fixes/open |
| tasks.json | 2010 | /v1/admin/tasks/all |
| punchlist.json | 13 | /v1/dashboard/punchList/all |
| supplements-open.json | 340 | /v1/admin/supplements/open |
| portal-kpi-summary.json | dict[16] | /v1/admin/reporting/kpis |
| portal-kpi-profit.json | dict[13] | /v1/admin/reporting/profit |
| portal-insurance-names.json | 55 | /v1/admin/reporting/insurance |
| leads.json | 320 | /v1/admin/leads |
| leads-employees.json | 200 | /v1/admin/leads/employees |
| finance-plans.json | 5 | /v1/admin/finance |

## Staged into `data/roofdocs-reference/`
pricing-templates(48) · pricing-contractor(718) · pricing-items(72) · dashboard-jobs-active(4409) · adjustments-open(22) · credits(139) · receivables-open

## Builders run on Mac (✓ local derived refresh)
- build-leads → leads-rollup.json (320 leads, 58 conversations, 15 reps)
- build-pricing-templates → pricing breakdown
- build-active-work → 596 active supplements / 68 carriers, $3.2M cross-sell, 327 overdue installs

## BLOCKED on Mac (need Windows box D:\RIQ21 OR Railway DB URL)
- **build-scheduling** + **audit-kpi-drift** — need full `data/projects.json` (16K lifetime jobs; only 4,409 active captured here)
- **backfill-intel-*** (events/fixes/tasks/punchlist/leads → Railway Postgres) — need DATABASE_URL. This is what updates the LIVE dashboards.

## TO COMPLETE LIVE UPDATE — pick one
1. Set Railway `DATABASE_URL` in env → run `backfill-intel-{events,fixes,tasks,punchlist,leads}.mjs` + `import-to-postgres.mjs` from Mac.
2. Ship `data/` + `data/roofdocs-reference/` dumps to `D:\RIQ21\data\` on omj21 and run the pipeline there (where projects.json + refresh-all live).

## Residual gaps
- **events-production** — re-grab (navigate Production calendar view in a passive session)
- New external adjusters for Susan KB (GM-confirmed, NOT portal staff): Lucy⭐, Sebastien, Alphonso, Sedjro, Rocky Parish, Debra Amoako, Branden Pritchett, Johan, Vince Wilson, Yusef. (Skip "Reg"/"Byron" — noisy.)
