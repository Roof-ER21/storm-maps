// TODO: NHP MESH swath overlay component
// - Renders MESH hail swath polygons on the map
// - Color-coded by hail size (green < 1" to purple > 4.5")
// - Click to see swath details (date, max size, area)
// - Fetches swath data from NHP API by date range

export default function HailSwathLayer() {
  // This component will render directly on the Google Map
  // using @vis.gl/react-google-maps overlay primitives
  return null;
}
